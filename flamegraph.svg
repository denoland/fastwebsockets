<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="262" onload="init(evt)" viewBox="0 0 1200 262" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:"Verdana"; font-size:12px; fill:rgb(0,0,0); }
#title { text-anchor:middle; font-size:17px; }
#search { opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
          svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            var el = frames.children;
            for(var i = 0; i < el.length; i++) {
                update_text(el[i]);
            }

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad - 100;
            matchedtxt.attributes.x.value = svgWidth - xpad - 100;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));
    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (/^ *\$/.test(txt) || t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                update_text(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                update_text(e);
            }
        }
    }
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
        update_text(el[i]);
    }
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="262" fill="url(#background)"/><text id="title" x="50.0000%" y="24.00">Flame Graph</text><text id="details" x="10" y="245.00"> </text><text id="unzoom" class="hide" x="10" y="24.00">Reset Zoom</text><text id="search" x="1090" y="24.00">Search</text><text id="matched" x="1090" y="245.00"> </text><svg id="frames" x="10" width="1180" total_samples="8945"><g><title>libdyld.dylib`tlv_get_addr (9 samples, 0.10%)</title><rect x="0.0000%" y="133" width="0.1006%" height="15" fill="rgb(227,0,7)" fg:x="0" fg:w="9"/><text x="0.2500%" y="143.50"></text></g><g><title>libsystem_platform.dylib`_platform_memmove (2 samples, 0.02%)</title><rect x="0.1006%" y="133" width="0.0224%" height="15" fill="rgb(217,0,24)" fg:x="9" fg:w="2"/><text x="0.3506%" y="143.50"></text></g><g><title>libdyld.dylib`tlv_get_addr (10 samples, 0.11%)</title><rect x="0.2571%" y="117" width="0.1118%" height="15" fill="rgb(221,193,54)" fg:x="23" fg:w="10"/><text x="0.5071%" y="127.50"></text></g><g><title>libsystem_kernel.dylib`__accept (1 samples, 0.01%)</title><rect x="0.3689%" y="117" width="0.0112%" height="15" fill="rgb(248,212,6)" fg:x="33" fg:w="1"/><text x="0.6189%" y="127.50"></text></g><g><title>socket`socket::main::_{{closure}}::_{{closure}} (3 samples, 0.03%)</title><rect x="0.3801%" y="117" width="0.0335%" height="15" fill="rgb(208,68,35)" fg:x="34" fg:w="3"/><text x="0.6301%" y="127.50"></text></g><g><title>libsystem_kernel.dylib`writev (4,398 samples, 49.17%)</title><rect x="0.4584%" y="101" width="49.1671%" height="15" fill="rgb(232,128,0)" fg:x="41" fg:w="4398"/><text x="0.7084%" y="111.50">libsystem_kernel.dylib`writev</text></g><g><title>libsystem_malloc.dylib`_malloc_zone_malloc (8 samples, 0.09%)</title><rect x="49.6255%" y="101" width="0.0894%" height="15" fill="rgb(207,160,47)" fg:x="4439" fg:w="8"/><text x="49.8755%" y="111.50"></text></g><g><title>libsystem_malloc.dylib`_nanov2_free (3 samples, 0.03%)</title><rect x="49.7149%" y="101" width="0.0335%" height="15" fill="rgb(228,23,34)" fg:x="4447" fg:w="3"/><text x="49.9649%" y="111.50"></text></g><g><title>libsystem_malloc.dylib`free (4 samples, 0.04%)</title><rect x="49.7485%" y="101" width="0.0447%" height="15" fill="rgb(218,30,26)" fg:x="4450" fg:w="4"/><text x="49.9985%" y="111.50"></text></g><g><title>libsystem_malloc.dylib`nanov2_malloc (7 samples, 0.08%)</title><rect x="49.7932%" y="101" width="0.0783%" height="15" fill="rgb(220,122,19)" fg:x="4454" fg:w="7"/><text x="50.0432%" y="111.50"></text></g><g><title>libsystem_platform.dylib`_platform_memmove (5 samples, 0.06%)</title><rect x="49.8714%" y="101" width="0.0559%" height="15" fill="rgb(250,228,42)" fg:x="4461" fg:w="5"/><text x="50.1214%" y="111.50"></text></g><g><title>socket`&lt;tokio::net::tcp::stream::TcpStream as tokio::io::async_read::AsyncRead&gt;::poll_read (3 samples, 0.03%)</title><rect x="49.9273%" y="101" width="0.0335%" height="15" fill="rgb(240,193,28)" fg:x="4466" fg:w="3"/><text x="50.1773%" y="111.50"></text></g><g><title>socket`DYLD-STUB$$free (1 samples, 0.01%)</title><rect x="49.9609%" y="101" width="0.0112%" height="15" fill="rgb(216,20,37)" fg:x="4469" fg:w="1"/><text x="50.2109%" y="111.50"></text></g><g><title>socket`DYLD-STUB$$malloc (3 samples, 0.03%)</title><rect x="49.9721%" y="101" width="0.0335%" height="15" fill="rgb(206,188,39)" fg:x="4470" fg:w="3"/><text x="50.2221%" y="111.50"></text></g><g><title>socket`DYLD-STUB$$writev (1 samples, 0.01%)</title><rect x="50.0056%" y="101" width="0.0112%" height="15" fill="rgb(217,207,13)" fg:x="4473" fg:w="1"/><text x="50.2556%" y="111.50"></text></g><g><title>libdyld.dylib`tlv_get_addr (1 samples, 0.01%)</title><rect x="50.3969%" y="85" width="0.0112%" height="15" fill="rgb(231,73,38)" fg:x="4508" fg:w="1"/><text x="50.6469%" y="95.50"></text></g><g><title>libsystem_c.dylib`DYLD-STUB$$__recvfrom (2 samples, 0.02%)</title><rect x="50.4080%" y="85" width="0.0224%" height="15" fill="rgb(225,20,46)" fg:x="4509" fg:w="2"/><text x="50.6580%" y="95.50"></text></g><g><title>libsystem_kernel.dylib`__recvfrom (4,004 samples, 44.76%)</title><rect x="50.4304%" y="85" width="44.7624%" height="15" fill="rgb(210,31,41)" fg:x="4511" fg:w="4004"/><text x="50.6804%" y="95.50">libsystem_kernel.dylib`__recvfrom</text></g><g><title>libsystem_malloc.dylib`_nanov2_free (12 samples, 0.13%)</title><rect x="95.1928%" y="85" width="0.1342%" height="15" fill="rgb(221,200,47)" fg:x="8515" fg:w="12"/><text x="95.4428%" y="95.50"></text></g><g><title>libsystem_platform.dylib`__bzero (1 samples, 0.01%)</title><rect x="95.3270%" y="85" width="0.0112%" height="15" fill="rgb(226,26,5)" fg:x="8527" fg:w="1"/><text x="95.5770%" y="95.50"></text></g><g><title>libsystem_platform.dylib`_platform_memset (1 samples, 0.01%)</title><rect x="95.3382%" y="85" width="0.0112%" height="15" fill="rgb(249,33,26)" fg:x="8528" fg:w="1"/><text x="95.5882%" y="95.50"></text></g><g><title>libdyld.dylib`tlv_get_addr (15 samples, 0.17%)</title><rect x="95.4612%" y="69" width="0.1677%" height="15" fill="rgb(235,183,28)" fg:x="8539" fg:w="15"/><text x="95.7112%" y="79.50"></text></g><g><title>socket`&lt;tokio::net::tcp::stream::TcpStream as tokio::io::async_read::AsyncRead&gt;::poll_read (43 samples, 0.48%)</title><rect x="95.3494%" y="85" width="0.4807%" height="15" fill="rgb(221,5,38)" fg:x="8529" fg:w="43"/><text x="95.5994%" y="95.50"></text></g><g><title>socket`tokio::runtime::io::registration::Registration::poll_ready (18 samples, 0.20%)</title><rect x="95.6288%" y="69" width="0.2012%" height="15" fill="rgb(247,18,42)" fg:x="8554" fg:w="18"/><text x="95.8788%" y="79.50"></text></g><g><title>socket`tokio::runtime::task::waker::clone_waker (4 samples, 0.04%)</title><rect x="95.7854%" y="53" width="0.0447%" height="15" fill="rgb(241,131,45)" fg:x="8568" fg:w="4"/><text x="96.0354%" y="63.50"></text></g><g><title>socket`socket::main::_{{closure}}::_{{closure}} (4,109 samples, 45.94%)</title><rect x="50.0168%" y="101" width="45.9363%" height="15" fill="rgb(249,31,29)" fg:x="4474" fg:w="4109"/><text x="50.2668%" y="111.50">socket`socket::main::_{{closure}}::_{{closure}}</text></g><g><title>socket`tokio::runtime::io::registration::Registration::poll_ready (11 samples, 0.12%)</title><rect x="95.8301%" y="85" width="0.1230%" height="15" fill="rgb(225,111,53)" fg:x="8572" fg:w="11"/><text x="96.0801%" y="95.50"></text></g><g><title>socket`tokio::runtime::task::raw::poll (8,548 samples, 95.56%)</title><rect x="0.4136%" y="117" width="95.5618%" height="15" fill="rgb(238,160,17)" fg:x="37" fg:w="8548"/><text x="0.6636%" y="127.50">socket`tokio::runtime::task::raw::poll</text></g><g><title>socket`tokio::runtime::io::registration::Registration::poll_ready (2 samples, 0.02%)</title><rect x="95.9530%" y="101" width="0.0224%" height="15" fill="rgb(214,148,48)" fg:x="8583" fg:w="2"/><text x="96.2030%" y="111.50"></text></g><g><title>libsystem_kernel.dylib`kevent (298 samples, 3.33%)</title><rect x="95.9754%" y="101" width="3.3315%" height="15" fill="rgb(232,36,49)" fg:x="8585" fg:w="298"/><text x="96.2254%" y="111.50">lib..</text></g><g><title>socket`std::time::Instant::now (1 samples, 0.01%)</title><rect x="99.3069%" y="101" width="0.0112%" height="15" fill="rgb(209,103,24)" fg:x="8883" fg:w="1"/><text x="99.5569%" y="111.50"></text></g><g><title>libsystem_c.dylib`clock_gettime (1 samples, 0.01%)</title><rect x="99.3069%" y="85" width="0.0112%" height="15" fill="rgb(229,88,8)" fg:x="8883" fg:w="1"/><text x="99.5569%" y="95.50"></text></g><g><title>libsystem_kernel.dylib`mach_absolute_time (1 samples, 0.01%)</title><rect x="99.3069%" y="69" width="0.0112%" height="15" fill="rgb(213,181,19)" fg:x="8883" fg:w="1"/><text x="99.5569%" y="79.50"></text></g><g><title>libdyld.dylib`tlv_get_addr (5 samples, 0.06%)</title><rect x="99.8211%" y="53" width="0.0559%" height="15" fill="rgb(254,191,54)" fg:x="8929" fg:w="5"/><text x="100.0711%" y="63.50"></text></g><g><title>socket`tokio::runtime::io::scheduled_io::ScheduledIo::wake (25 samples, 0.28%)</title><rect x="99.6423%" y="85" width="0.2795%" height="15" fill="rgb(241,83,37)" fg:x="8913" fg:w="25"/><text x="99.8923%" y="95.50"></text></g><g><title>socket`tokio::runtime::task::waker::wake_by_val (12 samples, 0.13%)</title><rect x="99.7876%" y="69" width="0.1342%" height="15" fill="rgb(233,36,39)" fg:x="8926" fg:w="12"/><text x="100.0376%" y="79.50"></text></g><g><title>socket`tokio::runtime::task::raw::schedule (4 samples, 0.04%)</title><rect x="99.8770%" y="53" width="0.0447%" height="15" fill="rgb(226,3,54)" fg:x="8934" fg:w="4"/><text x="100.1270%" y="63.50"></text></g><g><title>socket`tokio::runtime::scheduler::current_thread::_&lt;impl tokio::runtime::task::Schedule for alloc::sync::Arc&lt;tokio::runtime::scheduler::current_thread::Handle&gt;&gt;::schedule::_{{closure}} (3 samples, 0.03%)</title><rect x="99.8882%" y="37" width="0.0335%" height="15" fill="rgb(245,192,40)" fg:x="8935" fg:w="3"/><text x="100.1382%" y="47.50"></text></g><g><title>socket`tokio::runtime::io::Driver::turn (55 samples, 0.61%)</title><rect x="99.3181%" y="101" width="0.6149%" height="15" fill="rgb(238,167,29)" fg:x="8884" fg:w="55"/><text x="99.5681%" y="111.50"></text></g><g><title>socket`tokio::runtime::task::waker::wake_by_val (1 samples, 0.01%)</title><rect x="99.9217%" y="85" width="0.0112%" height="15" fill="rgb(232,182,51)" fg:x="8938" fg:w="1"/><text x="100.1717%" y="95.50"></text></g><g><title>socket`tokio::runtime::time::Driver::park_internal (359 samples, 4.01%)</title><rect x="95.9754%" y="117" width="4.0134%" height="15" fill="rgb(231,60,39)" fg:x="8585" fg:w="359"/><text x="96.2254%" y="127.50">sock..</text></g><g><title>socket`tokio::runtime::io::scheduled_io::ScheduledIo::wake (5 samples, 0.06%)</title><rect x="99.9329%" y="101" width="0.0559%" height="15" fill="rgb(208,69,12)" fg:x="8939" fg:w="5"/><text x="100.1829%" y="111.50"></text></g><g><title>all (8,945 samples, 100%)</title><rect x="0.0000%" y="213" width="100.0000%" height="15" fill="rgb(235,93,37)" fg:x="0" fg:w="8945"/><text x="0.2500%" y="223.50"></text></g><g><title>dyld`start (8,945 samples, 100.00%)</title><rect x="0.0000%" y="197" width="100.0000%" height="15" fill="rgb(213,116,39)" fg:x="0" fg:w="8945"/><text x="0.2500%" y="207.50">dyld`start</text></g><g><title>socket`main (8,945 samples, 100.00%)</title><rect x="0.0000%" y="181" width="100.0000%" height="15" fill="rgb(222,207,29)" fg:x="0" fg:w="8945"/><text x="0.2500%" y="191.50">socket`main</text></g><g><title>socket`std::rt::lang_start::_{{closure}} (8,945 samples, 100.00%)</title><rect x="0.0000%" y="165" width="100.0000%" height="15" fill="rgb(206,96,30)" fg:x="0" fg:w="8945"/><text x="0.2500%" y="175.50">socket`std::rt::lang_start::_{{closure}}</text></g><g><title>socket`std::sys_common::backtrace::__rust_begin_short_backtrace (8,945 samples, 100.00%)</title><rect x="0.0000%" y="149" width="100.0000%" height="15" fill="rgb(218,138,4)" fg:x="0" fg:w="8945"/><text x="0.2500%" y="159.50">socket`std::sys_common::backtrace::__rust_begin_short_backtrace</text></g><g><title>socket`socket::main (8,934 samples, 99.88%)</title><rect x="0.1230%" y="133" width="99.8770%" height="15" fill="rgb(250,191,14)" fg:x="11" fg:w="8934"/><text x="0.3730%" y="143.50">socket`socket::main</text></g><g><title>socket`tokio::runtime::time::_&lt;impl tokio::runtime::time::handle::Handle&gt;::process_at_time (1 samples, 0.01%)</title><rect x="99.9888%" y="117" width="0.0112%" height="15" fill="rgb(239,60,40)" fg:x="8944" fg:w="1"/><text x="100.2388%" y="127.50"></text></g><g><title>socket`core::ptr::drop_in_place&lt;[core::option::Option&lt;core::task::wake::Waker&gt; (1 samples, 0.01%)</title><rect x="99.9888%" y="101" width="0.0112%" height="15" fill="rgb(206,27,48)" fg:x="8944" fg:w="1"/><text x="100.2388%" y="111.50"></text></g><g><title> 32]&gt; (1 samples, 0.01%)</title><rect x="99.9888%" y="85" width="0.0112%" height="15" fill="rgb(225,35,8)" fg:x="8944" fg:w="1"/><text x="100.2388%" y="95.50"></text></g></svg></svg>